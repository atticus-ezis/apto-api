1. Create test where "owner" user is created and posts to a protected enpoint to verify cookie + csrf behavior
2. Create command to generate fake apartment data (use an existing Owner in the command)
